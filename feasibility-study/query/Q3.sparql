# Q3 — Models generated by training activities

PREFIX audit: <https://w3id.org/ai-audit#>
PREFIX prov:  <http://www.w3.org/ns/prov#>

SELECT DISTINCT ?trainingActivity ?model
WHERE {
  ?trainingActivity a audit:Training .
  ?model prov:wasGeneratedBy ?trainingActivity .
}
ORDER BY ?trainingActivity ?model
