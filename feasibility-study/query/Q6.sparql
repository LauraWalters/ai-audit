# Q6 — Dataset licenses (if available)

PREFIX audit:   <https://w3id.org/ai-audit#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT DISTINCT ?dataset ?license
WHERE {
  ?dataset a audit:Dataset .
  OPTIONAL { ?dataset dcterms:license ?license . }
}
ORDER BY ?dataset
